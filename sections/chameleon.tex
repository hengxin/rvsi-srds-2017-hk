% System Design of \chameleon{}

%%%%%%%%%%%%%%%
\begin{frame}{\chameleon{} Protocol}
  \begin{center}
    \chameleon{}: \\[10pt]
    A prototype \textbf{partitioned} \textbf{replicated} \\[6pt]
    distributed transactional \textbf{key-value} store
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{\chameleon{} Prototype}
  \begin{center}
    Classic \textbf{key-value} data model \\[4pt]
      Key: (row key, column key)
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{\chameleon{} Prototype}
  \fignocaption{width = 0.70\textwidth}{figs/chameleon-arch.pdf}

  \begin{center}
    \only<2>{Keys are \textbf{partitioned} within a single datacenter.}
    \only<3>{Each key is \textbf{replicated} in a master-slave manner across datacenters.}
    \only<4>{Transactions are first executed and committed on the masters,\\
    	and are then asynchronously propagated to slaves.}
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{\chameleon{} Protocol}
  \only<1-3, 5->{\fignocaption{width = 0.45\textwidth}{figs/chameleon-framework.pdf}}

  \begin{center}
    \only<2>{Partitioned replicated transactional key-value store}
    \only<3>{Client library}
    \only<5>{\rvsi{} protocol: \rvsims{} + \rvsimp{}}
  \end{center}

  \only<4>{
    \begin{figure}[t]
      \centering
      \input{algs/rvsi-code}
      \caption{Code snippet for writing \rvsi{} transactions.}
    \end{figure}}
\end{frame}
%%%%%%%%%%%%%%%
